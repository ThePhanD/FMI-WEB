function setDisplay() {
	var rows = 7;	// Get from the server
	var cols = 10;	// Get from the server
	createGrid(rows, cols);
}

function setAdminRoom() {
	// Get data from server
	
	// Set the room settings
}